{% extends 'base.html.twig' %}

{% block title %}Nous contacter
{% endblock %}

{% block body %}
	<main id="contact">
		<section class="widthDesktop lastSection">
			<div class="formulaire">
				<h1>Nous contacter</h1>

				{{ form_start (form)}}
				<div class="prenomNom">
					<div class="form-group">
					{{form_row(form.firstName, {
            		label: 'Prénom '
                   })}}
				</div>
				<div class="form-group">
					{{form_row(form.name, {
            		label: 'Nom',
                   })}}
				</div></div>
				<div class="form-group">
					{{form_row(form.email, {
            		label: 'Adresse e-mail',
                   })}}

				</div>
						<div class="form-group">
					{{form_row(form.message, {
            		label: 'Votre message',
					
                   })}}

				</div>

				{{ form_end (form)}}

			</div>
			<div class="imageFormulaire">
			<img src="{{asset('./img/abeilles-contact-576px.jpeg')}}" alt="bees"></div>
			</div>
		</section>
	</main>
{% endblock %}
